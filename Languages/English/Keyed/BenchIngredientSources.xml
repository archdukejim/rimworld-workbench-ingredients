<?xml version="1.0" encoding="utf-8"?>
<LanguageData>

  <!-- Mod settings -->
  <BIS.SettingsCategory>Bench Ingredient Sources</BIS.SettingsCategory>
  <BIS.DefaultRadius>Default fallback search radius: {0}</BIS.DefaultRadius>
  <BIS.DefaultRadius.Desc>Radius a new work table uses when it has no ingredient source selected. Pre-fills each bench's own fallback slider; changing it here does not alter benches already configured.</BIS.DefaultRadius.Desc>

  <!-- ITab button -->
  <BIS.Button>Ingredient sources</BIS.Button>

  <!-- Sources window -->
  <BIS.WindowTitle>Ingredient sources — {0}</BIS.WindowTitle>
  <BIS.StatusSourced>Sources selected: this bench pulls ingredients from the checked locations, map-wide. Each bill's own search radius is ignored.</BIS.StatusSourced>
  <BIS.StatusRadius>No source selected: this bench uses the fallback radius below for every bill, overriding each bill's own search radius.</BIS.StatusRadius>
  <BIS.SectionZones>Stockpile zones</BIS.SectionZones>
  <BIS.SectionBuildings>Storage buildings</BIS.SectionBuildings>
  <BIS.SectionGroups>Storage groups</BIS.SectionGroups>
  <BIS.None>(none on this map)</BIS.None>
  <BIS.ClearSelection>Clear selection</BIS.ClearSelection>
  <BIS.FallbackRadius>Fallback search radius (used when nothing is selected): {0}</BIS.FallbackRadius>

</LanguageData>
